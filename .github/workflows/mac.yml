name: macOS app

on:
  push:
    branches: [main, mac-version]
    tags: ["v*"]
    paths: ["mac/**", "SOUNDBOY/Assets/**", "SOUNDBOY/SOUNDBOY.csproj", ".github/workflows/mac.yml"]
  pull_request:
    paths: ["mac/**", ".github/workflows/mac.yml"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - name: Toolchain
        run: |
          xcodebuild -version
          swift --version

      - name: Unit tests (BPM/key, Rekordbox XML, EQ presets, playlists)
        working-directory: mac
        run: swift test

      - name: Build, sign and package
        env:
          MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
          MACOS_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
        run: bash mac/scripts/build.sh

      - name: UI snapshots (demo data)
        run: |
          mkdir -p mac/build/snapshots
          # Launch through LaunchServices (like a double-click) so the app gets a proper GUI session.
          # macOS has no `timeout`: poll, and stop it after 120 s if it hasn't quit itself.
          LOG="$PWD/mac/build/snapshots/app.log"
          open -n -W --env SOUNDBOY_PROFILE=ci --stdout "$LOG" --stderr "$LOG" mac/build/SOUNDBOY.app \
            --args --snapshot "$PWD/mac/build/snapshots" &
          pid=$!
          for i in $(seq 120); do kill -0 $pid 2>/dev/null || break; sleep 1; done
          if kill -0 $pid 2>/dev/null; then echo "app still running after 120 s, stopping it"; pkill -x SOUNDBOY || true; fi
          wait $pid || true
          echo "--- app log"; cat "$LOG" || true
          ls -la mac/build/snapshots

      - uses: actions/upload-artifact@v4
        with:
          name: SOUNDBOY-mac
          path: |
            mac/build/*.dmg
            mac/build/snapshots/*.png
            mac/build/snapshots/app.log
          if-no-files-found: warn

      - name: Attach DMG to the GitHub release
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "${GITHUB_REF_NAME}" mac/build/*.dmg --clobber
